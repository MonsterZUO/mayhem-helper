# dev-loop record · 2026-07-08 · tick 5

```
单元: C-dev#issue-2（大乱斗平衡参数）
发现:
- 数据源实测淘汰链：cdragon champions/{id}.json 无 balance → grok 答的
  modespecificdata/aram.bin.json 实为输入事件/任务（证伪，AI 搜索答案必须实测）→
  kiwi.bin.json（大乱斗内部代号）有 augment/VFX 无 per-champion 平衡、bin hash 不可稳定解析
  → 最终锁定 Fandom Module:ChampionData/data（MediaWiki API + 浏览器 UA 可过 403）
- wiki.gg 全线 403/错误页（连真浏览器都空）——该镜像不可用，别再试
- Lua 模块顶层条目是「\n  [\"名\"]」两空格缩进——split 正则漏缩进会 0 命中
改动: scripts/build-aram-balance.mjs（新）, src/assets/aram-balance.json（生成，142 英雄）,
      src/lib/aramBalance.ts（新）, src/views/MayhemCodexView.vue
VERIFY:
- 生成脚本 fail-loud 门（<50 中止），实跑 142  ✅
- pnpm type-check: 51 = main 基线，0 新增  ✅
- pnpm lint: 0 error  ✅ / pnpm build-only: 成功  ✅（均核 exit code）
- 金克丝 0.9/1.05 与 lolsantu「-10%/+5%」交叉印证  ✅
产出: PR #7（loop-dev，等人批）
remaining_delta: open enhancement issues 2（#2 待合入 / #4 未动）/ 待批 PR 1 / blocked 0
```
