# dev-loop record · 2026-07-08 · tick 3

```
单元: C-dev#issue-3（中文搜索强化）
发现:
- champion-summary zh_cn 字段语义：name=称号、description=正式名——「金克丝/暴走萝莉均命中」
  改动前已成立（既有 filter 匹配两字段），issue 真实增量 = 俗称表 + 称号·名字展示 + 海克斯过滤
- 拼音首字母确认不支持，按 issue「可选」定位不实现（避免引入拼音依赖/静态表维护）
- ⚠ VERIFY 假绿教训：worktree 忘 pnpm install，type-check 命令失败 → grep 不到 error TS
  → 误报 0；靠「0 vs 基线 51 反而更少」起疑 + 补 exit code 才抓到。
  修正：VERIFY 命令一律核 exit code，grep 计数不得单独当结论
改动: src/lib/championNicknames.ts（新增，69 俗称）,
      ChampionSelector.vue, MayhemBuild.vue, MayhemCodexView.vue
VERIFY:
- pnpm type-check（装依赖+删 tsbuildinfo 全量）: 51 errors = main 基线，改动文件 0 新增  ✅ exit 2 为既有报错
- pnpm lint: 0 error, exit 0  ✅
- pnpm build-only: 成功, exit 0  ✅
- 验收 rubric: 4 项全有结论（3 项实现有据，拼音项明确不做并说明）；UI 点击实测未验（同 tick1 原因）
产出: PR #6（loop-dev，等人批）
remaining_delta: open enhancement issues 3（#3 待合入）/ 待批 PR 1 / blocked 0
```
